pipeline {
    agent any
    environment{
        NOMBRE='rossana'
        APELLIDO= 'suarez'
        SECRET = credentials('TEST')
    }
    stages{
        stage ('checkout github'){
            steps{
                checkout scm
            }
        }   
    }
}